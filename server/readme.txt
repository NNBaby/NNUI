first, run Server.py
(note : 'host:port' = '127.0.0.1:5000')
if your Client is a python project, you can run Client.py to test the WebSever
if your Client is a uwp App, you can run the uiClient.
now, the webserver only surpports POST method and GET method 